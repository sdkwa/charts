apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-wafront
spec:
  selector:
    app: {{ .Release.Name }}-wafront
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: LoadBalancer
status:
  loadBalancer: {}